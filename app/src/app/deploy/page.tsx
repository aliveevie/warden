/**
 * Agent deployment wizard.
 * UI implementation delivered in PR-1 (Ika + Encrypt integration).
 */
export default function DeployPage() {
  return (
    <main>
      <h1>Deploy Agent</h1>
      <p>Coming in PR-1.</p>
    </main>
  );
}
