//! Local stub for `ika-solana-sdk-types`. The real crate exposes account-type
//! definitions and PDA helpers; warden-policy doesn't currently import any
//! symbols from it, but it's listed as a dependency so we keep an empty stub.

#![allow(unused)]
